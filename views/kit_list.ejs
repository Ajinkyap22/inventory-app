<!DOCTYPE html>
<html lang="en"></html>
<html>
  <head>
    <%- include('./partials/head') -%>
  </head>
  <body>
    <div class="container-fluid">
      <header><%- include('./partials/header'); -%></header>

      <main class="main">
        <!-- All kits in grid format with name & pic -->

        <h1><%= title %></h1>

        <ul>
          <!-- prettier-ignore -->
          <% if(kits.length) { %>
            <% kits.forEach(kit => { %>
          <li class="list-style-type-none">
            <a href="<%= kit.url %>" class="link-primary text-decoration-none""
              ><%= kit.name %></a
            >
          </li>
          <% }) } else { %>
          <li>There are no kits.</li>
          <% } %>
        </ul>
      </main>

      <footer><%- include('./partials/footer'); -%></footer>
    </div>
  </body>
</html>
