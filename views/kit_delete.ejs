<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head') -%>
  <body>
    <div class="container-fluid p-0">
      <header><%- include('./partials/header') -%></header>

      <main class="main mx-3">
        <h1><%= title %>: <%= kit.name %></h1>
        <p class="pb-3"><%= kit.description %></p>

        <div class="w-50 m-auto d-flex justify-content-center">
          <img src="/uploads/<%=kit.fileName%>" alt="team Logo" class="w-50" />
        </div>

        <form method="POST" action="">
          <p class="fw-bold pt-2">Are you sure you want to delete this kit?</p>

          <input
            type="hidden"
            class="form-control"
            id="kitid"
            name="kitid"
            required
            value="<%= kit._id %>"
          />

          <button class="btn btn-danger">Delete</button>
          <a href="/store/kits" class="link-secondary px-3 text-decoration-none"
            >Cancel</a
          >
        </form>
      </main>

      <footer class="position-absolute left-0 bottom-0 w-100">
        <%- include('./partials/footer') -%>
      </footer>
    </div>
  </body>
</html>
